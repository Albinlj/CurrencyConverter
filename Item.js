export default class Item {
  constructor(currencyCode, currencyName, rate, flagURL) {
    this.currencyCode = currencyCode;
    this.currencyName = currencyName;
    this.rate = rate;
    this.flagURL = flagURL;
  }
}
